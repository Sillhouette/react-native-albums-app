import React from 'react';
import { View } from 'react-native';

const Card = () => <View />;

const styles = {
  containerStyle: {
    borderWidth: 1,
    borderRadius: 2,
    borderColor: '#ddd',
    borderBottomWidth: 0
  }
};

export default Card;
